<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Contigo.SharedKernel\Contigo.SharedKernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Task E04/F01/US01/T02: this module's own AddBenchmarkModule(IServiceCollection)
         composition-root wiring (ServiceCollectionExtensions.cs) — ADR-002's "each module
         exposes an AddXxx(IServiceCollection) extension method", not yet done when this project
         was created (task E04/F01/US01/T01) because no host took a dependency on
         IBenchmarkService yet. These are generic .NET hosting/config abstractions, not a
         provider SDK, so they do not trip
         Contigo.ArchitectureTests.DependencyDirectionTests.Benchmark_module_must_not_reference_provider_sdks
         (Contigo.AiGateway already references the same-family packages for the same reason). -->
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" Version="10.0.11" />
    <!-- Task E04/F01/US02/T01 (fixture-adapter): this module's own AddBenchmarkModule(IServiceCollection)
         composition-root wiring (ServiceCollectionExtensions.cs) — ADR-002's "each module exposes an
         AddXxx(IServiceCollection) extension method". Same generic .NET hosting abstraction (not a
         provider SDK) Contigo.AiGateway.csproj / Contigo.Chat.csproj already reference for the
         identical reason. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.11" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
